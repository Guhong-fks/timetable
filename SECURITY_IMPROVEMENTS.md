# 课程表应用安全改进报告

## 已实施的安全改进

### 1. 存储安全改进
**文件**: `src/lib/storage.ts`

**改进内容**:
- 移除了无效的错误静默处理，改为显式的错误日志记录
- 增加了存储操作超时保护（10秒），防止后台恢复时阻塞
- 读取失败时清理损坏键，避免污染后续读取
- 写入失败时保留数据，避免误删有效数据
- 保持了 Web/原生平台的统一存储接口
- **数据存储方式**：JSON 明文写入 localStorage (Web) / AsyncStorage (原生)，无额外加密，确保应用重启、后台切回前台后课表数据可靠恢复

### 2. 输入验证统一化
**文件**: `src/lib/importers/timetable-importer.ts`

**改进内容**:
- 统一使用 `security.ts` 中的 `sanitizeText` 函数
- 移除了重复的验证函数
- 使用统一的 `validateFile` 函数进行文件验证
- 添加了资源限制器防止解析过程中的资源耗尽攻击

### 3. 资源限制实施
**文件**: `src/lib/importers/timetable-importer.ts`

**改进内容**:
- 在 `parseDocxTimetable` 函数中添加了 `ParseResourceLimiter`
- 实现了解析超时检查（30秒）
- 添加了单元格数量限制（200,000个）
- 添加了详细的错误处理和用户友好的错误消息

### 4. 依赖项安全更新
**文件**: `package.json`

**改进内容**:
- 移除 `xlsx` 库（规避原型污染漏洞 GHSA-4r6h / GHSA-5pgg）
- 升级 `mammoth` 到 1.11.0（修复目录遍历漏洞）
- 更新了其他有安全漏洞的依赖项
- 修复了 `decode-uri-component` 和 `uuid` 的漏洞
- 保持了与现有代码的兼容性

### 5. 文件类型限制收紧
**文件**: `src/lib/security.ts`、`src/lib/importers/timetable-importer.ts`

**改进内容**:
- 仅允许 `.docx` 格式导入（移除 `.xlsx` 支持）
- 文件大小限制 5MB (docx 专用)，10MB 兜底
- MIME 类型白名单校验

## 剩余的安全考虑

### 1. 高严重性漏洞（需要框架更新）
以下漏洞需要等待Expo框架更新才能完全修复：
- `@xmldom/xmldom` - XML注入漏洞
- `body-parser` - 拒绝服务漏洞
- `qs` - 原型污染漏洞
- `semver` - 正则表达式拒绝服务漏洞
- `send` - 模板注入漏洞
- `tar` - 路径遍历漏洞

**建议**: 定期运行 `npm audit` 检查，并在框架更新后及时应用修复。

### 2. 中等严重性漏洞
- `decode-uri-component` - 拒绝服务漏洞
- `uuid` - 缓冲区边界检查缺失

**当前状态**: 这些漏洞需要通过破坏性更改来修复，建议在下一个主要版本更新时处理。

## 安全最佳实践

### 1. 数据存储
- 存储操作带有超时保护和错误处理
- 读取失败时清理损坏键，写入失败时保留数据
- Web 使用 localStorage，原生使用 AsyncStorage，接口统一
- **当前版本使用明文 JSON 存储，依赖平台沙箱隔离，不额外加密，以保证数据在应用重启/后台恢复时的可用性**

### 2. 输入验证
- 所有用户输入都进行清理和验证
- 使用统一的验证函数确保一致性
- 实现资源限制防止滥用

### 3. 依赖项管理
- 定期运行安全审计
- 及时更新有漏洞的依赖项
- 移除已知有漏洞的库（如 xlsx）
- 使用语义化版本控制

### 4. 错误处理
- 记录详细的错误信息用于调试
- 向用户显示友好的错误消息
- 避免泄露敏感信息

## 建议的后续步骤

1. **定期安全审计**: 每月运行 `npm audit` 检查新的漏洞
2. **依赖项更新**: 在测试环境中验证新版本后再应用到生产环境
3. **安全培训**: 确保开发团队了解最新的安全最佳实践
4. **监控**: 设置监控系统检测异常行为
5. **备份**: 定期备份用户数据以防数据丢失

## 总结

本应用已实施了多项安全改进措施，包括存储错误处理与超时保护、输入验证统一化、资源限制、依赖项更新（移除 xlsx 库以规避原型污染漏洞）、文件类型限制收紧。当前版本使用明文 JSON 存储，依赖平台沙箱隔离，不额外加密，以确保应用重启、后台恢复时课表数据的可靠读取。虽然仍有一些需要框架更新才能修复的漏洞，但当前的安全措施已显著提高了应用的整体安全性。建议定期进行安全审计和依赖项更新，以保持应用的安全性。