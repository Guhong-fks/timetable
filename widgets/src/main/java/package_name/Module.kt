/*
    Placeholder Module.kt — intentionally empty.

    The native module ExpoWidgetsModule is provided by the
    @bittingz/expo-widgets library itself and linked into the app via
    Expo autolinking (the library ships its own
    expo.modules.widgets.ExpoWidgetsModule inside its AAR).

    This placeholder exists only to stop the expo-widgets config plugin
    from generating its template Module.kt into the app source tree,
    which would define the same fully-qualified class name and fail the
    build with a "Duplicate class" error. Keep this file empty (no
    package, no class declarations).
*/
